set nu #numbers
syntax on #different colors for syntax
set showmatch #shows matching parentheses

colorscheme delek #sets the scheme: to cycle thru all of the default themes, type ':colorscheme<space><tab>'

imap jk <esc> #mapping so I don't have to deal with esc
